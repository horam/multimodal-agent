# Multimodal Agent

A clean, modern multimodal AI agent using **Google Gemini**, built by **Horam**.

Supports:

- ✔ Text generation
- ✔ Image + text multimodal prompts
- ✔ Chat mode
- ✔ Async & Sync versions
- ✔ CLI tool (`agent`)
- ✔ Retry logic (handles model overloads)

---

## 🚀 Installation

Clone the repo:

```bash
git clone https://github.com/horam/multimodal-agent.git
cd multimodal-agent
```
